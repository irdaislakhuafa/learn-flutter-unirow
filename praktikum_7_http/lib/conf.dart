import 'package:flutter/material.dart';

class Conf {
  static final String API_BASE_URL = "https://reqres.in/api/users";
  static AppBar DEFAULT_BAR = AppBar(
    centerTitle: true,
    title: const Text("Irda Islakhu Afa | Http"),
  );
}
